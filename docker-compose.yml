version: "3.9"

services:
  naklet-frontend:
    build: .
    container_name: naklet-frontend-web
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:80"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
